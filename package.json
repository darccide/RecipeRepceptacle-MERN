{
	"name": "recipereceptacle",
	"version": "1.0.0",
	"description": "Social network for recipe creators",
	"main": "server.js",
	"scripts": {
		"server": "nodemon server",
		"start": "node server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Steven Thomson",
	"license": "MIT",
	"devDependencies": {
		"concurrently": "^4.1.2",
		"nodemon": "^1.19.2"
	}
}
